<fieldset class="search__filter search__date" data-prefix="<%= @prefix %>">
  <legend class="search__date-legend"><%= @legend %></legend>
  <%# The three parts share one border so they read as a single date field. %>
  <div class="search__date-parts">
    <% PARTS.each_with_index do |part, index| %>
      <% if index > 0 %>
        <span class="search__date-separator" aria-hidden="true">.</span>
      <% end %>
      <label class="visually-hidden" for="<%= field_id(part) %>"><%= label(part) %></label>
      <input
        type="text"
        id="<%= field_id(part) %>"
        name="<%= field_id(part) %>"
        value="<%= value(part) %>"
        placeholder="<%= placeholder(part) %>"
        inputmode="numeric"
        pattern="<%= part[:pattern] %>"
        title="<%= hint(part) %>"
        maxlength="<%= part[:maxlength] %>"
        autocomplete="off"
        <%# Password managers ignore autocomplete="off", so opt out with the
            vendor attributes: 1Password, LastPass, Bitwarden and Dashlane. %>
        data-1p-ignore
        data-lpignore="true"
        data-bwignore
        data-form-type="other"
        class="search__date-input <%= part[:modifier] %>">
    <% end %>
  </div>
</fieldset>
